import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cine.curator.pro',
  appName: 'Cine Curator Pro',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
